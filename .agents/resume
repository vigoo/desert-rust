#!/usr/bin/env bash
set -euo pipefail

export PATH="$HOME/.cargo/bin:$PATH"

for command in cargo cargo-fmt cargo-clippy cargo-deny mdbook; do
    if ! command -v "$command" >/dev/null 2>&1; then
        echo "Missing required command after orb resume: $command" >&2
        exit 1
    fi
done
